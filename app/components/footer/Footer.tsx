import { css } from "styled-system/css";

export const Footer = () => {
	return (
		<footer
			className={css({
				backgroundColor: "gray.800",
				color: "white",
			})}
		>
			Footer
		</footer>
	);
};
